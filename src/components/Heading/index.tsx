import React from "react";
import styles from "./Heading.module.css";

const Heading: React.FC = () => {
  return <div className={styles.container}>MICP Leaderboard | TPC</div>;
};

export default Heading;
